
function slots(num){
    for (var turns = 0; turns > 0 ; turns--) {
        num = (math.floor(math.random));
    }
slots(10)
// 1 quarter = 1 game
// console.log(Math.trunc(Math.random() * 50) + 51)
