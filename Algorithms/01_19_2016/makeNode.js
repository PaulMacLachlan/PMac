function Node(info) {
    return {
        thisData = info;
        thisNext = node;
}

CONSTRUCTOR FUNCTION

var neo = newNode('neo')


SLL()
 head = null

 addFront=
 isEmpty =

 bob.isEmpty() -> t / f
