var input = num
var arr = []

function sumToOne(num){
    while (num)
        i = 0
        Math.floor(num/10) = arr[i].push
        i++
        }
    while( arr.length < i ){
           arr[0] + arr[1] + arr[2]
    }

sumToOne(928)
