function removeBlanks(str){
    var l = string.length - 1
    for (var i = 0; i < l; i++) {
        if (str[i] === '/ /') {
            return;
        }
        else if (str[i] != '/ /') {
            var newStr = str[i];
        }
        console.log(newStr)l;
        }
    }
}
